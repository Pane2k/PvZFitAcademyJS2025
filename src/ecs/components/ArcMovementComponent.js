export default class ArcMovementComponent {
    constructor(vx, vy, gravity, targetY) {
        this.vx = vx;
        this.vy = vy;
        this.gravity = gravity;
        this.targetY = targetY; 
    }
}